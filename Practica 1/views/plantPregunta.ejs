<!DOCTYPE html>
<html lang="es">

<head>
    <title>Preguntas - Facebluff</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="../css/facebluff.css" />
</head>

<body>
    <img class=logo src="../img/Logo.png">
    <%- include("menuLogged") %>
    <p class=encPag><%= p.pregunta %></p>
    <form action="answerQuestion" method="POST">
    <% p.respuestas.forEach(r =>{ %>
        <div class=pregunta>
            <div><input type="radio" name="respuesta" value="<%= r %>"></div> 
            <div><%= r %></div>
        </div>
    <% }); %>
    <div class=preguntaExtra>
        <div><input type="radio" name="respuesta" value="extra"></div> 
        <div>Otra opción:</div>
        <input class=formTxt type="text" name="extraTxt">
    </div>
    <input type="hidden" name="idPregunta" value="<%= p.id %>">
    <div class=celdFormularioBtn>
        <input class=formBtn type="submit" value="Responder">
    </div>
    </form>
    
</body>


</html>