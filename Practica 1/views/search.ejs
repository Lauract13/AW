<!DOCTYPE html>
<html lang="es">

<head>
    <title>Busqueda - Facebluff</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="../css/facebluff.css" />
</head>

<body>
    <img class=logo src="../img/Logo.png">
    <%- include("menuLogged") -%>
    <p class=encPag>Resultados de búsqueda: '<%= search %>'</p>
    
    <div class=solAmistad>
            <% users.forEach((user) => { %>
                <% if (user.email !== myEmail) { %>
                    <form action="addFriend" method="POST">
                        <div class=celdSearch>
                            <div class=celdInsSearch><img class=imgAmigos src="/images/<%= user.image %>"></div>
                            <div class=celdInsSearch><label class=nombreSol><%= user.user %></label></div>
                            <input type="hidden" name="email" value="<%= user.email %>">
                            <div class=celdInsSearch><input class=amigosBtn type="submit" value="Solicitar Amistad"></div> 
                        </div>
                    </form>
                <% } %>
           <% }); %>
            
            
    </div>
    <%- include("error") %>
</body>


</html>